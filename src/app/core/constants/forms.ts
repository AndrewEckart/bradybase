export const PLACEHOLDERS = {
  statement: 'Prove: If $a$, $b$, $m$, $n$ are integers such that $m >1$, $n >= 1$, and $a \\equiv b \\pmod{m}$, then $a^n \\equiv b^n \\pmod{m}$.'
};

export enum Season {
  Autumn = 'Autumn',
  Spring = 'Spring',
  Summer = 'Summer',
  Winter = 'Winter'
}

export enum ProblemType {
  Exam = 'exam',
  HW = 'hw',
  Quiz = 'quiz'
}
